# Experiments

- Ledger: `artifacts/experiments/experiment.md`
- Aggregated tables (auto-export): `artifacts/experiments/experiment_results.tsv` and `artifacts/experiments/experiment_results.md`

To regenerate the tables after editing the ledger:

```bash
python scripts/export_experiment_results.py
```
