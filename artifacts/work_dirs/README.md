# Runs (work dirs)

Put training/evaluation outputs here, e.g.:

- `artifacts/work_dirs/E0002_smoke/`
- `artifacts/work_dirs/E0002_full_seed0/`

This repo does not vendor large run artifacts (`.pth`, `.pkl`, images, ZIPs, etc.). Keep only lightweight metrics/tables in git.
