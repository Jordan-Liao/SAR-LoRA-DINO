# Weights

This repo intentionally does not vendor large checkpoints.

## Backbone pretraining weights

Backbone pretraining weights are handled by `timm` when `pretrained=True`.

## Paper checkpoints

Trained SARDet-100K checkpoints (and other large artifacts) should be published via GitHub Releases:

- https://github.com/shatianming5/dino_sar/releases
