# Checkpoints

This directory is for large weights (pretrained backbones, trained checkpoints, LoRA adapters).

- Do **not** commit large files to git.
- Publish weights via Releases or external storage, and record links + hashes in the README / docs.

